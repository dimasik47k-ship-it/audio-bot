# 🎧 AudioTool Bot

Telegram-бот для редактирования тегов и конвертации аудиофайлов.

## 🚀 Установка

1. Скопируй ` .env.example` в `.env`
2. Вставь токен бота в `.env`
3. Установи зависимости:
   ```bash
   pip install -r requirements.txt